# Migrations

This directory is a scaffold only.

- `0001_init.sql` is copied from the current v1 schema design.
- Nothing in this repo applies migrations automatically yet.
- No AWS or database resources are created by these files.
